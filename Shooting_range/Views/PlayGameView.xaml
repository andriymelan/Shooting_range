<Window x:Class="Shooting_range.Views.PlayGameView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Shooting_range.Views"
        mc:Ignorable="d"
        Title="PlayGameView"  d:DesignHeight="1200" d:DesignWidth="1200" WindowState="Maximized" WindowStyle="None" Cursor="{Binding Crosshair}">
    <Window.Background>
        <ImageBrush ImageSource="../Images/PlayGameBackGround.jpg"/>
    </Window.Background>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="1300"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="700"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Border BorderBrush="Green" BorderThickness="2" Grid.Column="1" Grid.Row="1"/>
        <Grid Grid.Column="1" Grid.Row="1">
                <Canvas>
                <Ellipse Width="100" Height="100" Canvas.Top="{Binding CanvasTop}" Canvas.Left="{Binding CanvasLeft}">
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="{Binding Target}"/>
                        </Ellipse.Fill>
                        <Ellipse.InputBindings>
                            <MouseBinding
                            Command="{Binding TargetHitCommand}"
                            Gesture="leftclick"/>
                        </Ellipse.InputBindings>
                    </Ellipse>
                <Ellipse Width="100" Height="100" Canvas.Top="{Binding CanvasTop1}" Canvas.Left="{Binding CanvasLeft1}">
                    <Ellipse.Fill>
                        <ImageBrush ImageSource="{Binding Target}"/>
                    </Ellipse.Fill>
                    <Ellipse.InputBindings>
                        <MouseBinding
            Command="{Binding TargetHitCommand1}"
            Gesture="leftclick"/>
                    </Ellipse.InputBindings>
                </Ellipse>
                <Ellipse Width="100" Height="100" Canvas.Top="{Binding CanvasTop2}" Canvas.Left="{Binding CanvasLeft2}">
                    <Ellipse.Fill>
                        <ImageBrush ImageSource="{Binding Target}"/>
                    </Ellipse.Fill>
                    <Ellipse.InputBindings>
                        <MouseBinding
            Command="{Binding TargetHitCommand2}"
            Gesture="leftclick"/>
                    </Ellipse.InputBindings>
                </Ellipse>
            </Canvas>
            </Grid>
    </Grid>
</Window>
